#include "Pods-ESPPlaceholderTextView.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/ESPPlaceholderTextView" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/ESPPlaceholderTextView"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}